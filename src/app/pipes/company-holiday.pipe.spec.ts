import {CompanyHolidayPipe} from './company-holiday.pipe';

describe('CompanyHolidayPipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyHolidayPipe();
    expect(pipe).toBeTruthy();
  });
});
